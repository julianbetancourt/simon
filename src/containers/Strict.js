import React, { Component } from 'react';
import SettingsButton from '../components/SettingsButton';

class Strict extends Component {
  render() {
    return <SettingsButton type="strict" /> 
  }
}

export default Strict;
